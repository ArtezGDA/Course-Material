# setup
size(400, 400)
background(1)
stroke (0)

for j in range(9):
    # start position
    x = 10    
    y = 100 + (j * 25)    
    for i in range(6):
        x2 = x + (WIDTH - 20) / 6
        y2 = y + random(20) - 10
        line (x, y, x2, y2)
        x = x2
        y = y2
